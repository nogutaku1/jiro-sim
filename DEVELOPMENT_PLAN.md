# JIRO Sim 開発計画 v2

> **現状:** 単一HTML（1,900行・1.2MB）にゲーム全体が収まった、Cloudflare Pages + Supabase構成のブラウザゲーム。Ver 1.1。

---

## 🔴 Phase 0: 今すぐやるべきこと（リリースブロッカー）

最優先で対処すべき問題。これらが残っていると、バズったときにUX崩壊 or セキュリティ事故になる。

### 0.1 Supabase Row Level Security (RLS) の有効化
- **現状:** `anon_key` がクライアントJSに平文で埋まっている。RLSが無効の場合、誰でもランキングの改ざん・全消去が可能。
- **対応:** Supabaseダッシュボードで `rankings` テーブルに RLS を有効化し、`INSERT` のみ許可するポリシーを作成。`SELECT` は全員許可、`UPDATE/DELETE` は不可にする。
- **工数:** 15分

### 0.2 不要ファイルの削除
- **現状:** `src/engine.js`, `src/rhythm.js`, `src/scenes/*.js`, ルート直下の `engine.js`, `style.css` は使われていない。`index.html` に全て統合済み。
- **対応:** `src/` ディレクトリ、`engine.js`、`style.css` を削除。`title_bg.mp4`（4.8MB）が使われているか確認し、`title_bg_small.mp4`（394KB）で代替可能なら大きい方を削除。
- **理由:** デプロイサイズ削減。関係のないコードが PR に混在し続けると混乱の元。
- **工数:** 10分

### 0.3 OGP画像の設置
- **現状:** `<meta property="og:image" content="/assets/ogp.jpg" />` が指定されているが、実際に `ogp.jpg` が存在するか未確認。
- **対応:** 1200x630px の OGP 画像を作成し `/assets/ogp.jpg` に配置。X（Twitter）でシェアされたときの見た目を左右する最重要パーツ。
- **工数:** 30分

---

## 🟡 Phase 1: 体験の磨き込み（1〜2週間）

ゲームの「手触り」を良くして、初見プレイヤーの離脱率を下げる。新機能よりも、既存体験の完成度を上げることに集中する。

### 1.1 サウンド統一とBGM
- **現状:** リズムゲームのタップ音（スラープ/ミス）はWeb Audio APIで合成済み。行列待ちのガヤガヤ環境音も実装済み。だがリズムゲーム中のBGMが無い。
- **対応:**
  - Web Audio APIで軽量なビートBGMを生成（ドラム+ベース的なループ）
  - テンポをコンボ数に連動させる（コンボが増えるとBGMが加速 → 脳汁）
  - ゲームオーバー時に無音になる「静寂の恐怖」演出
- **効果:** 音があると「ゲームらしさ」が劇的に増す。無音のリズムゲームは致命的。

### 1.2 リズムゲームの判定エリア視認性向上
- **現状:** 黄色い判定ラインは描画されているが、タップすべき「ゾーン」が視覚的にわかりにくい。
- **対応:**
  - 判定ラインの付近にグロー付きの半透明帯（ゾーン）を追加
  - Perfect/Good/OK の範囲を色分けして表示
  - タップ成功時にパーティクルエフェクトを追加
- **効果:** 「どこでタップすればいいか」が直感的にわかるようになり、初見のストレスが激減。

### 1.3 チュートリアルの強化
- **現状:** リズムゲーム開始前にオーバーレイ説明があるが、テキストベースで実際の操作感がわからない。
- **対応:**
  - 最初の3〜5ノーツをスローモーションで落とし「ここでタップ！」と矢印UIで誘導
  - 成功したら通常速度に移行
- **効果:** 「やり方がわからなくて即ゲームオーバー → 離脱」のパターンを防止。

---

## 🟢 Phase 2: バイラル設計（2〜3週間）

SNS拡散を「偶然」ではなく「仕組み」にする。

### 2.1 リザルト画像の動的生成
- **現状:** テキストのみのXシェア。タイムラインで埋もれやすい。
- **対応:**
  - Canvas APIでリザルトカードを生成（スコア、称号、コール内容、背景はラーメン画像）
  - `canvas.toBlob()` → `navigator.share()` で画像付きシェア（対応端末のみ、非対応はテキストフォールバック）
- **なぜ重要:** 画像付きツイートはテキストのみの **約3倍のエンゲージメント** を得る。これがバズの起爆剤。
- **工数:** 1〜2日

### 2.2 「ロット乱し」プレッシャー演出
- **現状のゲームオーバーは淡白。** 失敗しても「お腹の限界…」と表示されて終わり。ドラマがない。
- **対応:**
  - ミス連続時に画面の縁が赤く脈動
  - 店主のセリフが画面上部に出現（「ロット乱す気か？」「急げ！」）
  - ゲームオーバー画面で「ロット乱しで店内が凍りつきました」的なナラティブ
- **シェア効果:** 「ロット乱しで公開処刑されたｗｗ」→ 自虐シェアが最もバイラルしやすい。
- **工数:** 2〜3日

### 2.3 隠し要素（イースターエッグ）
- **アイデア:**
  - コール画面で全部「なし」を選ぶと「素ラーメンの修行僧」称号
  - 券売機で特定ボタンを連打 →「鍋JIRO」「つけ麺」の隠しメニュー出現
  - 全マシマシ完食で「天空の破壊者」的な特別エンディング
- **なぜ有効:** 「これ知ってた？」型の発見報告はリツイートされやすい。攻略情報が自然発生する。
- **注意:** 隠し要素はゲーム本体の体験を損ねないこと。あくまでおまけ。
- **工数:** 1〜2日

---

## 🔵 Phase 3: コミュニティと継続性（1ヶ月〜）

一度バズった後、プレイヤーが「戻ってくる理由」を作る。

### 3.1 リアルタイム接続人数の表示
- **概要:** Supabase Realtime または簡易的なポーリングで「現在○○人がJIROに並んでいます」を表示。
- **表示場所:** タイトル画面、行列待ちシーン。
- **注意:** 実装コストと効果のバランスを見て判断。Supabase Realtimeは無料枠の制限あり。ポーリング（30秒間隔）の方が現実的。
- **工数:** 1日

### 3.2 週間ランキングリセット
- **現状:** ランキングが永久蓄積。上位が固定されると新規プレイヤーのモチベが死ぬ。
- **対応:** Supabase側でスケジュールジョブ（pg_cron等）を使い、週次でランキングをリセット or 週間/月間/全期間のタブ切替を実装。
- **工数:** 半日

### 3.3 店舗バリエーション
- **概要:** 背景画像・店主のセリフ・BGMのテイストが異なる「店舗」をランダム or 選択制で追加。
- **効果:** 「〇〇店が出た！」的なガチャ性で毎回飽きない。
- **注意:** 画像の追加は base64 エンコードされた index.html のサイズを肥大化させる。外部画像URLへの移行を先に検討すべき。

---

## ⚠️ Manusの元案に対する指摘

### 却下 or 大幅修正した提案

| 元案 | 判断 | 理由 |
|------|------|------|
| 状態管理の純粋関数化 | **解決済み** | `Object.assign({}, diffSettings[...])` でコピーしてからミュートしており、既に対応済み。 |
| タイマー処理の堅牢化 | **解決済み** | `clearAllTimers()` で `setTimeout`, `setInterval`, `requestAnimationFrame` 全てクリアしており、`stopAmbientBGM()` も統合済み。 |
| レガシーコード削除 | **Phase 0 に移動** | 「1〜2週間かけるフェーズ1」ではなく「10分で終わる掃除」。Phase 0 に格上げ。 |
| 隠しコマンドの「鍋JIRO」 | **縮小** | 新モードを丸ごと作るのは工数に見合わない。称号やセリフ分岐程度のライトな隠し要素に留める。 |
| リアルタイム人数表示 | **後回し** | バズる前に作っても「0人が並んでいます」と表示されて逆効果。Phase 3 に移動。 |

### 元案に不足していた視点

| 項目 | 指摘 |
|------|------|
| **セキュリティ** | RLS未設定のSupabaseはランキング改ざんし放題。元案で一切言及なし。 |
| **OGP** | シェアの「見た目」を左右する最重要要素なのに計画に含まれていない。 |
| **BGM** | リズムゲーム中のBGMが無い問題に触れていない。音ゲーなのに。 |
| **画像サイズ** | index.html が 1.2MB（大部分が base64 画像）。今後機能を足すなら外部化が必要。 |
| **週間ランキング** | 長期運用でランキングが固定化する問題への対策がない。 |

---

## スケジュール

| Phase | 期間 | ゴール |
|-------|------|--------|
| **Phase 0** | すぐ（1時間以内） | RLS有効化、不要ファイル削除、OGP画像配置 |
| **Phase 1** | 1〜2週間 | BGM追加、判定ゾーン視認性改善、チュートリアル強化 |
| **Phase 2** | 2〜3週間 | リザルト画像生成、ロット乱し演出、隠し要素 |
| **Phase 3** | バズ後 | 接続人数表示、週間ランキング、店舗バリエーション |

> **原則:** Phase 2 の「リザルト画像生成」が最もROIが高い。ここを最速で実装すべき。
