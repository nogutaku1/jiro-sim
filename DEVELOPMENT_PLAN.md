# JIRO Sim 今後の開発計画

本ドキュメントは、JIRO Simのコードレビュー結果およびバズる仕掛けの提案に基づき、今後の開発マイルストーンを整理したものです。

## 1. フェーズ1：コードベースのクリーンアップと安定化（短期目標）

現在の単一HTMLファイル（`index.html`）の利点を活かしつつ、保守性と安定性を高めるためのリファクタリングを行います。

### 1.1. レガシーコードの整理
- **対象:** `src/` ディレクトリ以下の古いモジュール（`engine.js`, `rhythm.js`, `scenes/` など）
- **アクション:** 現在の `index.html` に統合されたロジックと重複・競合しているため、これらのファイルを削除、または別ブランチ（例：`legacy-modules`）に退避させます。
- **目的:** 状態管理（`gameState`）の不整合を防ぎ、コードの見通しを良くします。

### 1.2. 状態管理の純粋関数化
- **対象:** `sceneEatingRhythm` 内の難易度調整ロジック
- **アクション:** `diffSettings` オブジェクトを直接ミューテート（破壊的変更）している箇所を修正し、毎プレイ開始時に初期状態から難易度を再計算する純粋関数として切り出します。
- **目的:** リトライ時に難易度や状態が正しくリセットされないバグを未然に防ぎます。

### 1.3. タイマーと非同期処理の堅牢化
- **対象:** `clearAllTimers()` および `requestAnimationFrame` のキャンセル処理
- **アクション:** シーン遷移時のフラグ管理を厳密にし、非同期処理が競合して予期せぬ挙動を引き起こさないようにします。

---

## 2. フェーズ2：バズる仕掛けの実装（中期目標）

SNSでのバイラル拡散を狙い、プレイヤーの感情を揺さぶる新機能を追加します。

### 2.1. 「ロット乱し」ペナルティシステム
- **概要:** リズムゲーム中、画面端に「隣の客の進捗ゲージ」を表示します。
- **演出:** 自分の進行が遅れると、店主からのプレッシャー演出（画面の赤点滅、BGMのテンポアップなど）が強くなります。
- **シェア要素:** ミスを連発してゲームオーバーになった際、「ロット乱しで公開処刑されました」といった自虐的なシェアテキストを生成し、話題性を喚起します。

### 2.2. 隠しコマンド（裏メニュー）の導入
- **概要:** 特定の操作で出現するイースターエッグを実装します。
- **演出:** 券売機の特定箇所を3回タップで「鍋二郎（テイクアウト）」モード、またはコール時に隠しボタンで「麺マシ」を選択可能にします。
- **シェア要素:** 「隠し要素を見つけた！」というプレイヤーの発見の喜びが、自発的なシェアを生み出します。

---

## 3. フェーズ3：シェア機能とコミュニティ機能の強化（長期目標）

プレイヤー同士の競争心や一体感を高め、継続的なプレイを促します。

### 3.1. リザルト画面の動的画像生成
- **概要:** テキストだけでなく、視覚的にインパクトのある画像を生成してシェアできるようにします。
- **実装:** Canvas APIを使用し、最終スコア、コール内容、獲得した称号（例：「全マシマシの覇者」「ロットクラッシャー」など）を一枚の派手な画像（ラーメンの背景付き）に合成します。
- **目的:** タイムライン上でのインプレッションとクリック率を劇的に向上させます。

### 3.2. リアルタイム「並び」人数の表示（疑似マルチプレイ）
- **概要:** Supabaseなどのバックエンドを活用し、現在プレイしている（並んでいる）他のプレイヤー数を表示します。
- **演出:** タイトル画面や行列待ちシーンで、「現在〇〇人が全国のJIROに並んでいます」と表示します。
- **目的:** プレイヤーに一体感と競争心を与え、「今〇〇人待ちなんだけどｗｗ」といった実況的なシェアを促します。

---

## 4. スケジュール概要

| フェーズ | 期間目安 | 主なタスク |
| :--- | :--- | :--- |
| **フェーズ1** | 1〜2週間 | レガシーコード削除、状態管理リファクタリング、タイマー処理修正 |
| **フェーズ2** | 2〜3週間 | ロット乱しゲージ実装、プレッシャー演出、隠しコマンド実装 |
| **フェーズ3** | 3〜4週間 | Canvas画像生成ロジック、Supabase連携によるリアルタイム人数表示 |

この計画に沿って開発を進めることで、JIRO Simはより安定し、かつSNSで爆発的に拡散されるポテンシャルを秘めたゲームへと進化します。
